/** The device's own setting. Use `useColorScheme` from the theme context instead
 * unless you specifically need to bypass the user's override. */
export { useColorScheme as useSystemColorScheme } from 'react-native';
